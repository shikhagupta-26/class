pipeline {
    agent any
    stages {
        stage('Install') {
            steps { 
            bat 'npm install' 
            echo "Installing..."
            }
        }
        stage('build') {
            steps { 
            bat 'npm run ng -- build' 
            echo "Building..."
            }
        }
        stage('deploy') {
            steps { 
            bat 'serve -s build' 
            echo "Deploying..."
         }
        }
       
    }
}
