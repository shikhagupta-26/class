pipeline {
    agent any
    stages {
        stage('Install') {
            steps { 
            bat 'npm install' 
            echo "Installing..."
            }
        }
        stage('build') {
            steps { 
            bat 'npm run ng -- build' 
            echo "Building..."
            }
        }
       
    }
}
